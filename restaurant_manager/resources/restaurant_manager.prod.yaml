restaurant_manager:
  database: "${DATABASE_URL}"
  jwt:
    private_key_path: "${JWT_PRIVATE_KEY_PATH}"
    public_key_path: "${JWT_PUBLIC_KEY_PATH}"
  aws:
    profile: "${AWS_PROFILE}"
    region: "${AWS_REGION}"
  qr_template: "https://api.servu.com.co/orders?restaurant_id=%s&status=%s&table_id=%s"